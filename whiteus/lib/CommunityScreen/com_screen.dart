import 'package:flutter/material.dart';

class ComScreen extends StatelessWidget {
  const ComScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: const Text("Community Screen"),
      ),
    );
  }
}
